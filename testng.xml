<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<!--
    Конфигурационный файл TestNG для запуска тестов Playwright.

    Основные настройки:
    - parallel="classes" - параллельное выполнение тестовых классов
    - thread-count="3" - количество параллельных потоков

    Слушатели:
    - PlaywrightPageInjector - обеспечивает внедрение объектов Page и Page Objects
    - PlaywrightTestFactory - обрабатывает аннотации @UsePage для настройки тестов

    Пример запуска:
    ```
    mvn clean test -DsuiteXmlFile=testng.xml
    ```

    Для запуска конкретного теста:
    ```
    mvn clean test -DsuiteXmlFile=testng.xml -Dtest=HomePageTest
    ```
-->
<suite name="PlaywrightTestSuite" parallel="classes" thread-count="3">
    <!-- Регистрация слушателей для обработки аннотаций и внедрения зависимостей -->
    <listeners>
        <listener class-name="com.listeners.PlaywrightPageInjector"/>
        <listener class-name="com.listeners.PlaywrightTestFactory"/>
    </listeners>

    <!-- Определение набора тестов для запуска -->
    <test name="HomePage Tests" parallel="classes" thread-count="3">
        <classes>
            <class name="com.tests.HomePageTest"/>
            <!-- Добавьте другие тестовые классы здесь -->
        </classes>
    </test>
</suite>